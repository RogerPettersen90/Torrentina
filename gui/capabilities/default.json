{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window: core APIs plus event listen/emit.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
